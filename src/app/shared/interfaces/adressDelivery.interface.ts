export interface Pickup {
    value: string;
    viewValue: string;
    disabled?: boolean;
  }

  export interface ITimes {
    value: string;
    viewValue: string;
  }

  export interface Street {
    value: string;
    viewValue: string;
    disabled?: boolean;
  }